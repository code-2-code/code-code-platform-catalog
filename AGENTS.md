# Agent Rules

- This repository owns catalog, support reference data, model registry, and CLI runtime catalog behavior only.
- Do not edit protobuf source or generated contracts here; change `code-code-contracts` first.
- Do not add auth, provider orchestration, agent runtime, profile, notification, or deployment behavior here.
- Keep changes scoped to model, support, or CLI runtime catalog domains.
- Run the catalog service tests before delivery.
